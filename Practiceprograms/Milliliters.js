//convert liters into milli liters
//in this method the inputs is liters
//we get the output as milliliters 
function litresConversion(liters) {
    var milliliters = liters * 1000;
    console.log(milliliters);
}
litresConversion(35);
litresConversion(15);
litresConversion(5);
