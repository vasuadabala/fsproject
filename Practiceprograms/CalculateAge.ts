//7. Write a program to calculate age of a person
// we take the input as number and it give us output

function calculateAge() {
  let presentyear = 2025;
  let birthyear = 2000;
  let presentage = presentyear - birthyear;
  console.log(presentage);
}

calculateAge();
