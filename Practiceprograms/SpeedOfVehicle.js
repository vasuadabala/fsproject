//13. Write a function to calculate speed of the vehicle with distance and time as inputs.
function calculateSpee() {
    var distance = 20; //20 kilometers
    var time = 2; // 2 hour
    var vehiclespeeed = distance / time;
    console.log(vehiclespeeed);
}
calculateSpeed();
